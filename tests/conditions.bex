if(
    int(input()),
    exec(
        assign(f, 1),
        say("non-zero input")
    ),
    exec(
        assign(f, 0),
        say("zero input")
    )
)

say(f)

if(
    int(input()),
    say("works only if input is non-zero")
)
